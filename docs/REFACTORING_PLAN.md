# Task Master Pro - 目录重构行动计划

本文档提供了一个具体的、分阶段的计划，以优化Task Master Pro的目录结构。

---

## 📊 现状评估

### 问题分析

1. **文件重复**
   - `TM_COMMANDS_GUIDE.md` - 3个位置（`.claude/`、`assets/`、`dist/`）
   - `CLAUDE.md` - 2个位置（主、`.taskmaster/`）✓ 已妥善（导入关系）

2. **文档散落**
   - 用户文档：`/docs/`
   - 项目文档：`/.taskmaster/docs/`
   - 参考文档：`/context/`
   - 上下文：`CLAUDE.md`

3. **遗留代码未迁移**
   - `/src/` - 根级工具（620K）
   - `/scripts/` - 遗留脚本（1.0M）
   - `/mcp-server/` - 已弃用（528K）

4. **过大的外部参考**
   - `/context/` - MCP协议文档（1.2M）

5. **构建产物污染**
   - `/dist/` - 自动生成，不应提交

---

## 🎯 优化目标

- ✅ 消除文件重复
- ✅ 统一文档位置
- ✅ 完成遗留代码迁移
- ✅ 移除已弃用的代码
- ✅ 优化外部文档管理
- ✅ 确保`.gitignore`规则正确

---

## 📅 分阶段实施计划

### 第1阶段：信息架构（当前 ✓）

**时间：** 1-2天
**交付物：**
- ✅ `docs/DIRECTORY_STRUCTURE.md` - 标准目录结构
- ✅ `docs/GITIGNORE_RULES.md` - .gitignore规则说明
- ✅ `docs/REFACTORING_PLAN.md` - 本文档

**完成状态：** ✅ 已完成

---

### 第2阶段：清理和整理（优先级高）

**时间：** 2-3天
**任务：**

#### 任务2.1：修复TM_COMMANDS_GUIDE.md重复
```bash
# 目前位置：
# - .claude/TM_COMMANDS_GUIDE.md （保留）
# - assets/claude/TM_COMMANDS_GUIDE.md （重复）
# - dist/assets/claude/TM_COMMANDS_GUIDE.md （构建产物，自动生成）

# 行动：
# 1. ✅ 确认.claude/版本是正确的
# 2. ✅ 在assets/中删除重复（使用git rm）
# 3. ✅ 更新任何指向assets/版本的引用指向.claude/版本
```

**命令：**
```bash
# 检查两个版本是否相同
diff .claude/TM_COMMANDS_GUIDE.md assets/claude/TM_COMMANDS_GUIDE.md

# 如果相同，删除assets版本
git rm assets/claude/TM_COMMANDS_GUIDE.md

# 提交
git commit -m "cleanup: remove duplicate TM_COMMANDS_GUIDE.md from assets"
```

#### 任务2.2：更新.gitignore规则
```bash
# 已完成的更改：
# ✅ 改进Cursor配置注释，说明为什么跟踪

# 需要验证的规则：
# 确保所有文档类型未被忽略
# 确保编辑器配置正确跟踪
```

**命令：**
```bash
# 验证重要文件是否被跟踪
git ls-files | grep -E '\.(md|json)$' | head -20

# 检查任何意外的忽略
git status --ignored | head -20
```

#### 任务2.3：创建docs/README.md导航文件
```markdown
# 文档结构

## 用户文档
- 快速开始
- 命令参考
- 配置指南
- 最佳实践

## 项目文档
- 架构
- 分支策略
- 目录结构
- Git规则

## 指南
- 如何贡献
- 迁移指南
```

**文件：** `/docs/README.md`

---

### 第3阶段：遗留代码迁移（优先级中）

**时间：** 3-5天
**目标：** 将`/src/`和`/scripts/`中的所有代码迁移到`@tm/core`

#### 任务3.1：审计遗留代码
```bash
# 1. 列出src/中的所有模块
find src/ -type f -name "*.js" -o -name "*.ts"

# 2. 列出scripts/中的所有模块
find scripts/ -type f -name "*.js" -o -name "*.ts"

# 3. 为每个模块确定所有权
# - 任务管理 → @tm/core/modules/tasks/
# - 配置管理 → @tm/core/modules/config/
# - Git操作 → @tm/core/modules/git/
# - AI集成 → @tm/core/modules/ai/
# - 存储 → @tm/core/modules/storage/
# 等等
```

#### 任务3.2：为每个模块创建迁移计划
```
例如 src/ai-providers/:
1. 在@tm/core/modules/ai/中创建等效类
2. 编写单元测试
3. 更新CLI使用新类
4. 更新MCP使用新类
5. 运行集成测试
6. 删除src/ai-providers/
7. 创建changeset
```

#### 任务3.3：逐步迁移
- 每周迁移1-2个模块
- 每个迁移都应有测试覆盖
- 创建单独的PR用于每个模块

**状态跟踪：**
在`.taskmaster/docs/MIGRATION-ROADMAP.md`中更新进度

---

### 第4阶段：移除已弃用代码（优先级中）

**时间：** 1-2天

#### 任务4.1：确认mcp-server/已被替换
```bash
# 检查是否有任何代码引用mcp-server/
grep -r "mcp-server" --include="*.js" --include="*.ts" \
  --exclude-dir=node_modules --exclude-dir=.git \
  apps/ packages/

# 确认apps/mcp/已完全取代mcp-server/
ls -la apps/mcp/src/tools/ | wc -l  # 应该有36+工具

# 如果确认无引用，删除目录
git rm -r mcp-server/
git commit -m "chore: remove deprecated mcp-server directory (replaced by apps/mcp)"
```

#### 任务4.2：清理agents/目录
```bash
# 目前agents/只有todo-hook-manager.js
# 考虑：
# 1. 保留并在docs中说明用途
# 2. 或移动到更合适的位置（如packages/claude-code-plugin/agents/）

# 建议：保留在agents/，因为这是特殊的钩子管理器
```

---

### 第5阶段：优化外部参考（优先级低）

**时间：** 1-2天

#### 任务5.1：评估context/目录
```bash
# context/目录包含：
# - MCP协议文档（1.2M - 太大！）
# - 聊天记录存档
# - 参考文档

# 选项：
# A. 移到外部Wiki
# B. 压缩为.tar.gz
# C. 保留但在.gitignore中压缩
# D. 链接到官方文档

# 建议：
# - 保留必要的MCP_INTEGRATION.md
# - 删除重复的MCP文档（链接到官方源）
# - 移除聊天记录存档（不是代码）
```

**命令：**
```bash
# 如果选择删除大型MCP文档
cd context/
rm -f fastmcp-*.txt mcp-*.txt mcp-*.json

# 创建README说明删除原因
cat > README.md << 'EOF'
# Reference Documentation

This directory contains reference materials:
- MCP_INTEGRATION.md - Local MCP integration notes
- chats/ - Archived conversations (informational)

For full MCP specification, see: https://spec.modelcontextprotocol.io/
EOF

git add .
git commit -m "cleanup: remove duplicate MCP protocol docs, keep only references"
```

---

### 第6阶段：验证和测试（所有阶段）

**时间：** 每个阶段1天

#### 检查清单
- ✅ 所有npm脚本仍然工作
- ✅ CLI命令仍然工作
- ✅ MCP工具仍然工作
- ✅ 所有测试通过
- ✅ 文档都有效
- ✅ 构建成功

**命令：**
```bash
# 完整验证
npm run build                    # 应该成功
npm test                        # 所有测试通过
TAMP list                       # 应该工作
npm run mcp-server              # 应该启动

# 检查文档
ls -la docs/                    # 所有文档存在
cat docs/DIRECTORY_STRUCTURE.md # 可读
```

---

## 📝 每阶段提交和changeset

### 提交模式
```bash
# 阶段1：信息架构
git add docs/DIRECTORY_STRUCTURE.md docs/GITIGNORE_RULES.md
git commit -m "docs: add directory structure and gitignore rules documentation"
npm run changeset  # patch

# 阶段2.1：清理重复
git rm assets/claude/TM_COMMANDS_GUIDE.md
git commit -m "cleanup: remove duplicate TM_COMMANDS_GUIDE.md"
npm run changeset  # patch

# 阶段3.1-3.3：代码迁移
# 每个模块一个PR
git commit -m "refactor: migrate [module-name] to @tm/core"
npm run changeset  # patch或minor（取决于变化）

# 阶段4：移除遗留代码
git rm -r mcp-server/
git commit -m "chore: remove deprecated mcp-server (replaced by apps/mcp)"
npm run changeset  # patch
```

---

## 🎯 完成标准

每个阶段完成时应满足：

✅ **第1阶段**
- [ ] 文档已创建
- [ ] GitHub上已评审
- [ ] 团队理解新结构

✅ **第2阶段**
- [ ] 文件重复已移除
- [ ] .gitignore已更新
- [ ] git status显示干净
- [ ] 所有测试通过

✅ **第3阶段**
- [ ] src/和scripts/为空（除了必要的遗留代码）
- [ ] 所有功能在@tm/core中
- [ ] CLI和MCP指向新位置
- [ ] 100%测试覆盖

✅ **第4阶段**
- [ ] mcp-server/已删除
- [ ] 没有悬挂的引用
- [ ] 构建成功

✅ **第5阶段**
- [ ] context/已优化
- [ ] 大文件已移除或压缩
- [ ] 文档仍然可访问

---

## 📊 预期结果

### 目录大小改进

**现在：**
```
apps/           2.5M  ✓ 最优
packages/       1.9M  ✓ 最优
tests/          1.8M  ✓ 最优
dist/           1.3M  ❌ 应忽略
context/        1.2M  ? 可优化
scripts/        1.0M  ❌ 迁移中
src/            616K  ❌ 迁移中
mcp-server/     528K  ❌ 已弃用
docs/           512K  ✓ 最优
assets/         328K  ✓ 最优

总计：~11M（包括可移除的）
```

**完成后：**
```
apps/           2.5M  ✓ 最优
packages/       2.5M  ✓ 已扩展（从src/和scripts/）
tests/          1.8M  ✓ 最优
docs/           512K  ✓ 最优
assets/         328K  ✓ 最优
context/        100K  ✓ 已优化
.git/           ~2M   标准大小

总计：~10M（已清理）
可移除：dist/, src/, scripts/, mcp-server/ = -3M
**最终：~7M干净代码**
```

---

## 🚀 实施建议

### 快速路径（2周）
1. **第1周**
   - ✅ 第1阶段（信息架构）
   - ✅ 第2阶段（清理）
   - ✅ 第4阶段（移除已弃用）

2. **第2周**
   - ✅ 第5阶段（优化外部参考）
   - ✅ 第6阶段（验证）

### 完整路径（1个月）
- 第1-6阶段顺序进行
- 允许彻底的代码迁移
- 完全的测试覆盖

---

## ✅ 已完成的项目

- ✅ 第1阶段：信息架构
  - ✅ `docs/DIRECTORY_STRUCTURE.md` 已创建
  - ✅ `docs/GITIGNORE_RULES.md` 已创建
  - ✅ `.gitignore` 已更新

- ✅ 第2阶段：清理和整理
  - ✅ Task 2.1：移除 `assets/claude/TM_COMMANDS_GUIDE.md` 重复
  - ✅ Task 2.2：验证 .gitignore 规则
  - ⏸️ Task 2.3：创建 docs/README.md 导航文件（待做）

- ✅ 第4阶段：移除已弃用代码
  - ✅ Task 4.1：删除 `mcp-server/` 目录（528K 代码已移除）
  - ✅ Task 4.2：验证无悬挂引用

- ✅ 第5阶段：优化外部参考
  - ✅ Task 5.1：删除重复的 MCP 文档（1.1M 已移除）
  - ✅ 创建 `context/README.md` 链接到官方文档
  - ✅ 保留 `MCP_INTEGRATION.md` 作为遗留参考
  - ✅ 保留 `chats/` 存档

**项目大小改进：**
- 之前：~11M（包括可移除部分）
- 现在：已减少 ~1.6M（528K mcp-server + 1.1M 文档）
- 后续：删除 src/ (616K) 和 scripts/ (1.0M) 时再减少 1.6M

---

## ⏭️ 后续步骤

1. **立即完成（已完成）**
   - ✅ 第1-2阶段：信息架构和清理
   - ✅ 第4-5阶段：移除遗留和优化外部参考

2. **短期（待做）**
   - Task 2.3：创建 docs/README.md 导航文件
   - Task 3：遗留代码迁移规划（src/ 和 scripts/ → @tm/core）
   - Task 6：验证和测试所有功能

3. **中期（1个月内）**
   - 逐步迁移 src/ 和 scripts/ 到 @tm/core
   - 完全清理项目结构

4. **长期（进行中）**
   - 监控新代码遵循新结构
   - 定期审查目录布局
   - 更新文档以反映变化

---

*此计划应每两周审查一次，以确保进度和调整优先级*

最后更新：2025-11-11 - 第2、4、5阶段已完成，项目大小减少1.6M
