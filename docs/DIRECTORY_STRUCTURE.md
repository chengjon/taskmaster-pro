# Task Master Pro - 科学的目录结构规划

本文档定义了Task Master Pro项目的标准目录结构和文件组织方式。

---

## 📋 目录结构总览

```
taskmaster-pro/
├── .github/                    # GitHub配置
├── .changeset/                 # 版本变更日志
├── .cursor/                    # Cursor编辑器配置
├── .kiro/                      # Kiro (Cursor) 集成配置
├── .taskmaster/                # 项目任务管理
├── .vscode/                    # VS Code工作区配置
├── .claude/                    # Claude Code集成
├── agents/                     # AI代理和Hook管理器
├── apps/                       # 应用程序（CLI、MCP、文档、插件）
├── assets/                     # 可复用资源（模板、配置示例）
├── bin/                        # 可执行脚本
├── context/                    # 外部参考文档（只读）
├── docs/                       # 用户文档和指南
├── packages/                   # 共享包和核心库
├── scripts/                    # 构建和开发脚本（遗留，迁移中）
├── src/                        # 根级工具库（遗留，迁移中）
├── tests/                      # 测试套件
├── CHANGELOG.md                # 版本日志
├── CLAUDE.md                   # Claude Code主文档（自动加载）
├── GITHUB_UPDATE_GUIDE.md      # GitHub同步指南
├── LICENSE                     # MIT + Commons Clause许可证
├── README.md                   # 项目说明
├── biome.json                  # 代码格式化配置
├── jest.config.js              # Jest测试配置
├── package.json                # npm工作区配置
├── tsconfig.json               # TypeScript配置
├── tsdown.config.ts            # esbuild构建配置
└── turbo.json                  # Turborepo管道配置
```

---

## 📁 详细目录说明

### 1. 根目录配置文件

**职责：** 项目级配置和文档

| 文件/目录 | 用途 | 何时编辑 |
|-----------|------|--------|
| `CLAUDE.md` | Claude Code主上下文（自动加载） | 更新项目架构或开发指南 |
| `.taskmaster/CLAUDE.md` | Task Master AI集成指南 | 更新任务管理工作流 |
| `README.md` | 项目主文档 | 功能发布或文档更新 |
| `package.json` | npm工作区配置 | 添加/删除包或改变依赖 |
| `tsconfig.json` | TypeScript配置 | 改变TS编译目标 |
| `turbo.json` | Turborepo任务管道 | 添加新构建步骤 |
| `biome.json` | 代码格式化规则 | 改变代码风格 |
| `jest.config.js` | Jest测试配置 | 改变测试框架设置 |
| `tsdown.config.ts` | esbuild构建配置 | 改变构建目标 |
| `LICENSE` | 许可证 | 不修改（MIT + Commons Clause） |
| `CHANGELOG.md` | 发布历史 | 自动生成，通过changesets |

**原则：** 仅保留必要的项目级配置，应用级配置放在`apps/`中

---

### 2. GitHub集成 (`.github/`)

**职责：** GitHub Actions、PR模板、Issue模板

```
.github/
├── workflows/              # 自动化工作流
│   ├── ci.yml             # 持续集成
│   ├── release.yml        # 自动发布
│   ├── extension-ci.yml   # 插件CI
│   └── ...
├── PULL_REQUEST_TEMPLATE/ # PR模板
│   ├── feature.md
│   ├── bugfix.md
│   ├── integration.md
│   └── config.yml
├── ISSUE_TEMPLATE/        # Issue模板
│   ├── bug_report.md
│   ├── feature_request.md
│   └── feedback.md
└── scripts/               # GitHub脚本
    ├── release.mjs
    ├── check-pre-release-mode.mjs
    └── ...
```

**管理规则：**
- 工作流文件命名：`{feature}-{type}.yml`
- 所有自动化脚本使用`.mjs`（ECMAScript modules）
- 不在此目录中存储项目代码

---

### 3. 版本管理 (`.changeset/`)

**职责：** 管理语义化版本和变更日志

```
.changeset/
├── config.json            # Changeset配置
├── README.md              # 使用指南
└── *.md                   # 每个PR的变更条目
```

**工作流：**
```bash
npm run changeset          # 创建变更条目
npm run release            # 发布新版本（自动生成CHANGELOG）
```

---

### 4. 编辑器集成

#### 4.1 Cursor编辑器 (`.cursor/`)

```
.cursor/
├── mcp.json               # Cursor的MCP服务器配置
├── rules/                 # Cursor特定规则
│   ├── ai_providers.mdc
│   ├── architecture.mdc
│   ├── dev_workflow.mdc
│   └── ...
└── ...
```

#### 4.2 Kiro自动化 (`.kiro/`)

```
.kiro/
├── hooks/                 # Cursor IDE自动化钩子（7个）
│   ├── tm-code-change-task-tracker.kiro.hook
│   ├── tm-complexity-analyzer.kiro.hook
│   ├── tm-daily-standup-assistant.kiro.hook
│   ├── tm-git-commit-task-linker.kiro.hook
│   ├── tm-pr-readiness-checker.kiro.hook
│   ├── tm-task-dependency-auto-progression.kiro.hook
│   └── tm-test-success-task-completer.kiro.hook
├── steering/              # Kiro AI工作流规则
│   ├── dev_workflow.md
│   ├── kiro_rules.md
│   ├── self_improve.md
│   └── taskmaster.md
└── settings/              # Kiro配置
    └── mcp.json
```

#### 4.3 Claude Code集成 (`.claude/`)

```
.claude/
├── settings.json          # Claude Code工具许可列表
├── commands/              # 自定义slash命令
│   ├── dedupe.md
│   └── taskmaster-*.md
└── ...
```

#### 4.4 VS Code工作区 (`.vscode/`)

```
.vscode/
├── settings.json          # 工作区设置
├── extensions.json        # 推荐的插件
├── launch.json            # 调试配置
├── tasks.json             # 任务定义
└── mcp.json              # VS Code的MCP配置
```

---

### 5. 任务管理系统 (`.taskmaster/`)

**职责：** Task Master Pro项目管理数据和文档

```
.taskmaster/
├── CLAUDE.md              # Task Master AI工作流指南
├── config.json            # 模型配置（TAMP models命令）
├── state.json             # 运行时状态
├── tasks/                 # 任务数据
│   ├── tasks.json         # 任务数据库（auto-managed）
│   ├── task_*.txt         # 个别任务文件（auto-generated）
│   └── ...
├── docs/                  # 项目文档
│   ├── prd.txt           # 主产品需求文档
│   ├── prd-tm-start.txt  # 启动PRD
│   ├── MIGRATION-ROADMAP.md
│   ├── autonomous-tdd-git-workflow.md
│   ├── tdd-workflow-phase-*.md
│   ├── research/         # AI研究笔记（自动生成）
│   └── templates/        # PRD模板
├── reports/              # 分析报告
│   └── task-complexity-report*.json
└── templates/            # 模板文件
    ├── example_prd.txt
    └── example_prd_rpg.txt
```

**重要：**
- 不要手动编辑`tasks.json`和自动生成的任务文件
- 使用`TAMP`命令管理任务
- `.taskmaster/config.json`通过`TAMP models`命令管理

---

### 6. 应用程序 (`apps/`)

**职责：** 独立的应用和服务

```
apps/
├── cli/                   # 命令行应用 (@tm/cli)
│   ├── src/
│   │   ├── command-registry.ts
│   │   ├── commands/      # CLI命令实现
│   │   ├── lib/           # CLI工具库
│   │   ├── ui/            # 终端UI组件
│   │   └── utils/         # CLI工具
│   ├── tests/             # CLI测试（集成和单元）
│   ├── package.json
│   └── tsconfig.json
├── mcp/                   # MCP服务器 (@tm/mcp)
│   ├── src/
│   │   ├── index.ts       # 入口点
│   │   ├── tools/         # MCP工具实现（36+个）
│   │   └── shared/        # 共享工具
│   ├── package.json
│   └── tsconfig.json
├── docs/                  # 文档站点（Mintlify）
│   ├── getting-started/   # 快速开始指南
│   ├── best-practices/    # 最佳实践
│   ├── capabilities/      # 功能说明
│   ├── tdd-workflow/      # TDD工作流
│   ├── package.json
│   └── vercel.json
└── extension/             # VS Code插件（未来）
    ├── src/
    │   ├── extension.ts   # 插件入口
    │   ├── components/    # VS Code组件
    │   ├── webview/       # Webview实现
    │   └── services/      # 插件服务
    ├── package.json
    └── tsconfig.json
```

**原则：**
- 每个应用有自己的`package.json`和`tsconfig.json`
- 应用级测试放在`apps/{app}/tests/`
- 应用级文档放在`apps/{app}/docs/`（如存在）

---

### 7. 共享包 (`packages/`)

**职责：** npm工作区中的可复用库

```
packages/
├── tm-core/               # 核心业务逻辑（CRITICAL）
│   ├── src/
│   │   ├── modules/       # 领域模块（DDD）
│   │   │   ├── tasks/     # 任务管理域
│   │   │   ├── auth/      # 认证域
│   │   │   ├── workflow/  # 工作流编排
│   │   │   ├── git/       # Git操作域
│   │   │   ├── config/    # 配置管理
│   │   │   ├── integration/
│   │   │   ├── storage/   # 存储抽象
│   │   │   ├── ai/        # AI提供商
│   │   │   ├── execution/ # 任务执行
│   │   │   ├── reports/   # 报告生成
│   │   │   ├── commands/  # 命令执行
│   │   │   └── ui/        # UI渲染
│   │   └── common/        # 共享工具
│   │       ├── mappers/   # 数据转换
│   │       ├── errors/    # 错误定义
│   │       ├── types/     # 共享类型
│   │       └── logger/    # 日志服务
│   ├── tests/             # 测试
│   │   ├── unit/
│   │   ├── integration/
│   │   └── e2e/
│   └── docs/              # 架构文档
├── tm-bridge/             # 遗留代码桥接 (@tm/bridge)
├── claude-code-plugin/    # Claude Code插件
│   ├── commands/          # 50+自定义slash命令
│   └── agents/            # AI代理定义
├── build-config/          # 构建配置共享
├── ai-sdk-provider-grok-cli/  # 自定义AI SDK提供商
└── other-packages/        # 其他共享包
```

**架构原则（来自CLAUDE.md）：**
- **所有业务逻辑必须在`@tm/core`中**
- CLI和MCP是**薄包装层**，只负责参数解析和格式化
- 不要在CLI或MCP中重复业务逻辑

---

### 8. 资源和模板 (`assets/`)

**职责：** IDE集成、配置示例和模板

```
assets/
├── claude/                # Claude Code资源
│   └── TM_COMMANDS_GUIDE.md  # 命令参考
├── config.json            # 配置示例
├── env.example            # 环境变量模板
├── example_prd.txt        # PRD模板
├── example_prd_rpg.txt    # RPG游戏PRD示例
├── gitignore              # .gitignore模板
├── AGENTS.md              # AI代理文档
├── GEMINI.md              # Gemini特定说明
├── scripts_README.md      # 脚本说明
├── kiro-hooks/            # Kiro钩子模板
└── rules/                 # Cursor/Roo规则
```

---

### 9. 文档 (`docs/`)

**职责：** 用户文档和项目指南

```
docs/
├── README.md              # 文档导航
├── DIRECTORY_STRUCTURE.md # 本文件
├── BRANCHES.md            # 分支策略
├── architecture/          # 架构文档
│   ├── PROJECT-EVOLUTION.md
│   └── OPTIMIZATION-A-IMPLEMENTATION-SUMMARY.md
├── guides/                # 用户指南
│   ├── CONTRIBUTING.md
│   ├── CLAUDE_CODE_PLUGIN.md
│   └── llms-install.md
├── examples/              # 使用示例
│   ├── claude-code-usage.md
│   └── codex-cli-usage.md
├── providers/             # 提供商文档
│   ├── codex-cli.md
│   └── gemini-cli.md
├── standards/             # 代码标准
│   └── FILE_ORGANIZATION_RULES.md
├── zh-cn/                 # 中文文档
│   ├── GETTING-STARTED-CN.md
│   ├── CLAUDE-CN.md
│   ├── ARCHITECTURE-ANALYSIS-CN.md
│   └── OPTIMIZATION-ROADMAP-CN.md
├── archived/              # 存档文档
│   └── README-task-master.md
└── scripts/               # 文档脚本
    └── models-json-to-markdown.js
```

**原则：**
- 用户文档在`/docs/`
- 项目管理文档在`/.taskmaster/docs/`
- 应用文档在`apps/{app}/docs/`
- 包文档在`packages/{package}/docs/`

---

### 10. 测试 (`tests/`)

**职责：** 项目级测试套件

```
tests/
├── unit/                  # 单元测试
│   ├── scripts/
│   ├── ai-providers/
│   ├── prompts/
│   ├── profiles/
│   ├── progress/
│   ├── mcp/
│   ├── mcp-providers/
│   └── ui/
├── integration/           # 集成测试
│   ├── cli/
│   ├── mcp-server/
│   ├── providers/
│   └── profiles/
├── e2e/                   # 端到端测试
│   ├── workflows/
│   ├── fixtures/          # 测试数据
│   └── utils/             # 测试工具
├── manual/                # 手动测试指南
│   ├── prompts/
│   └── progress/
├── fixtures/              # 共享测试数据
├── helpers/               # 共享测试帮助函数
└── README.md              # 测试指南
```

**测试最佳实践（从CLAUDE.md）：**

优先级顺序：
1. **Co-located tests**（推荐）
   - `src/<file>.spec.ts` - 单元测试
   - `tests/integration/<module>/<file>.test.ts` - 集成测试

2. **集中式测试**（备选）
   - `tests/unit/packages/<package>/`
   - `tests/e2e/`

规则：
- 测试文件使用`.ts`扩展名（不是`.js`）
- 单元测试：`*.spec.ts`
- 集成测试：`*.test.ts`
- 只有在测试实际异步操作时才使用`async/await`

---

### 11. 遗留代码 (`src/` 和 `scripts/`)

**职责：** 正在迁移到`@tm/core`的旧代码

```
src/                      # 根级工具（遗留）
├── ai-providers/
├── constants/
├── profiles/
├── prompts/
├── provider-registry/
├── schemas/
├── ui/
└── utils/

scripts/                  # 遗留脚本
├── dev.js               # 遗留CLI入口
├── init.js              # 初始化脚本
├── modules/             # 遗留模块实现
└── ...
```

**迁移计划：**
1. 识别所属域（tasks、config、git等）
2. 在`@tm/core`的相应域中创建等效方法
3. 添加测试
4. 更新CLI和MCP使用新方法
5. 移除旧代码

---

### 12. 外部文档 (`context/`)

**职责：** 只读参考文档

```
context/
├── chats/                 # 对话记录存档
├── fastmcp-core.txt      # FastMCP文档
├── mcp-*.txt             # MCP协议文档
├── mcp-protocol-schema-*.json
└── MCP_INTEGRATION.md    # MCP集成指南
```

**原则：**
- 此目录仅用于参考
- 不要修改这些文件
- 大文件不应提交，改用引用链接

---

### 13. 根级文件 (`bin/`)

**职责：** 可执行脚本和入口点

```
bin/
├── task-master.js        # 旧CLI入口（现为packages/tm-cli）
└── tm.js                 # CLI二进制脚本
```

---

## 🚫 应该从Git中移除的内容

根据`.gitignore`评审，以下应该被**忽略**但当前可能被跟踪：

1. **`dist/`** - 构建输出（自动生成）
2. **`.turbo/`** - Turborepo缓存
3. **`node_modules/`** - 依赖项
4. **`coverage/`** - 测试覆盖报告
5. **`.jest/`** - Jest缓存
6. **`.eslintcache`** - Linter缓存
7. **`.DS_Store`** - macOS文件
8. **`*.log`** - 日志文件

## ✅ 应该在Git中的文件

以下文件应该被**跟踪**，不应该被忽略：

| 文件/目录 | 原因 |
|-----------|------|
| 所有`.md`文档 | 这是代码的一部分 |
| `docs/` | 用户文档 |
| `.taskmaster/docs/` | 项目文档 |
| `.claude/commands/` | 团队共享的命令 |
| `.cursor/rules/` | 编辑器规则 |
| `.kiro/` | 自动化配置 |
| `assets/` | 共享资源 |
| `context/` | 参考文档（可选压缩） |
| 所有配置文件 | 项目配置 |

---

## 📊 目录大小参考

| 目录 | 大小 | 优化建议 |
|------|------|--------|
| `apps/` | 2.5M | 最大，包含文档站点 |
| `packages/` | 1.9M | 核心代码 |
| `tests/` | 1.8M | 测试覆盖 |
| `dist/` | 1.3M | ❌ 应忽略，不提交 |
| `context/` | 1.2M | 参考文档，可存储外部 |
| `scripts/` | 1.0M | 遗留代码，逐步迁移 |
| `src/` | 616K | 遗留代码，逐步迁移 |
| `mcp-server/` | 528K | ❌ 已弃用，考虑移除 |
| `docs/` | 512K | 用户文档 |
| `assets/` | 328K | 共享资源 |

---

## 🔄 迁移步骤

### 第1步：清理`.gitignore`
- 确保不必要的文档不被忽略
- 移除已弃用项目的条目

### 第2步：统一文档位置
- 用户文档 → `/docs/`
- 项目文档 → `/.taskmaster/docs/`
- 参考文档 → `/context/` 或外部链接

### 第3步：完成从`scripts/`和`src/`的迁移
- 每个遗留函数映射到`@tm/core`中的等效项
- 更新CLI和MCP指向新实现
- 移除旧代码

### 第4步：移除已弃用的目录
- `mcp-server/` - 已被`apps/mcp/`取代
- 旧的`agents/`条目

### 第5步：优化大型外部文档
- `context/`中的MCP文档太大（1.2M）
- 创建外部参考链接而不是嵌入完整文档

---

## 📝 文件命名约定

### 文档文件
- README指南 → `{FEATURE}-CN.md`（中文）
- 架构文档 → `{COMPONENT}-ARCHITECTURE.md`
- API文档 → `{COMPONENT}-API.md`
- 指南 → `{TOPIC}-GUIDE.md`
- 存档 → `archived/{OLD_NAME}.md`

### 配置文件
- 公共配置 → `{tool}.json` 或 `{tool}.config.js`
- 示例 → `{tool}.example.json`
- 项目级 → `.{tool}/config.json`

### 脚本文件
- 构建脚本 → `scripts/build-{feature}.js`
- 开发脚本 → `scripts/dev-{feature}.js`
- 工具脚本 → `scripts/tools-{feature}.js`

---

## 🎯 目标状态

- ✅ 清晰的目录层级（3-4层深度）
- ✅ 没有重复的文件或目录
- ✅ 明确的所有权和责任
- ✅ 易于导航和维护
- ✅ 最小化构建产物
- ✅ 最大化文档可发现性

---

*本文档应与项目结构同步更新。最后更新：2025-11-11*
